MEMORY {
	RAM:  start = $0000, size = $EEFF;
	ROM:  start = $F000, size = $1000, fillval=$FF;
}

SEGMENTS {
	RAM_CODE: load = RAM, type = ro, start = $9000;
	LOADER_CODE: load = RAM, type = ro, start = $ED00;
}
